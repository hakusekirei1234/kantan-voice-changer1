# 変更履歴

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠。
バージョン番号は [セマンティックバージョニング](https://semver.org/lang/ja/)。

## [1.0.1] — 2026-08-13

### 修正

- **内蔵ボイチェンのピッチ（声の高さ）が一切変わらなかった問題を修正。**

  TD-PSOLA でピッチを動かすのは「入力から切り出す間隔（解析ホップ）と、
  出力に並べる間隔（合成ホップ）を変える」ことだけなのに、v1.0.0 は両方を
  同じ `Hs = T0 / P` で進めていた。歩幅が同じということは WSOLA の恒等変換で、
  グレイン内リサンプル（フォルマント）だけが効き、ピッチは 1 セントも動かなかった。

  修正後は解析側が入力の自然周期 `T0` ごとに進み、合成側だけが `Hs = T0 / P` で進む。
  同じグレインを 2 度出せばピッチが上がり、1 つ飛ばせば下がる。

- **フォルマントのつまみがピッチも動かしていた問題を修正。**

  窓の長さを「出力側で 2*T0」と決めていたため、フォルマント倍率 `F` を掛けた
  実際の切り出し長が `2*T0*F` になり、`F > 1` では隣の声門パルスまでグレインに
  入り込んでいた。入った隣パルスは `F` 倍に詰まった間隔で並ぶので、
  フォルマント +12 半音でピッチがちょうど 1 オクターブ上がっていた。
  窓の長さを入力側で `T0` に固定し、隣のパルスが窓のゼロ点に載るようにした。

- **グレインの位相が声門パルスに載っていなかった問題を修正。**

  窓が隣のパルスを消せるのは、窓の中心がパルスに載っているときだけ。
  相関ロックは「前のグレインとの相対位相」しか保てないので、最初にずれた位相は
  そのまま固定されていた。結果、同じ設定でも入力の位相次第で変換が効いたり
  効かなかったりした。毎グレイン、直前 1 周期の短時間エネルギーが最大の位置へ
  中心を載せ直すようにした。探索は過去向きだけなので遅延は増えない。

- **OLA の正規化が窓を打ち消していた問題を修正。**

  出力サンプルごとに「積算した窓の和」で割っていたため、重なりの薄い場所ほど
  強く持ち上がり、重なりがゼロの場所では `w*x / w = x` となって窓が完全に
  打ち消されていた。窓の役目は隣の声門パルスを消すことなので、これが打ち消されると
  上の 3 つを直してもピッチ変換は成立しない。
  ゲインはグレインごとに `Hs / 窓の積分値` で解析的に決めるようにした。

### 変更

- PSOLA の先読みを **512 → 768 サンプル**（10.7 ms → 16.0 ms）に変更。
  内蔵エンジン全体の遅延は 768 → 1024 サンプル（16.0 ms → 21.3 ms）になる。

  PSOLA はグレインに声門パルスを前後 1 周期ぶん収める必要があり、120 Hz の男声では
  それだけで 800 サンプルになる。512 では窓が足りない。
  合成母音との対数スペクトル距離で測った値:

  | 条件 | 512 | **768** | 1024 |
  |---|---|---|---|
  | 120 Hz +12 半音 | 1.26 dB | **0.81 dB** | 0.76 dB |
  | 150 Hz 変換なし | 0.55 dB | **0.21 dB** | 0.21 dB |
  | 90 Hz +7 半音 | 1.14 dB | **0.79 dB** | 0.78 dB |

  768 から先はほとんど改善しないので、ここを打ち止めとした。
  外部プラグイン（Pitchproof 等）を使う場合はこの先読みは経路に入らないので、
  遅延は v1.0.0 から変わらない。

- 相関探索の範囲を ±96 → ±32 サンプルに縮小。参照を毎回「前の解析マーク」に
  取り直すので誤差が溜まらず、広い探索が要らなくなった。先読み予算に直接効く。

### 追加

- **DSP の自動テスト** (`tests/`)。合成母音を作って通し、出力の F0・スペクトル包絡・
  振幅の揺れを測る。ピッチが動くこと、フォルマントとピッチが直交すること、
  遅延が設定で動かないこと、ブロック長で結果が変わらないことなどを検査する。

  ```
  cmake -S . -B build -DKVC_BUILD_TESTS=ON
  cmake --build build --config Release --target kvc_tests
  ctest --test-dir build -C Release --output-on-failure
  ```

- **GitHub Actions の CI** (`.github/workflows/ci.yml`)。
  push と pull request のたびに、テスト → アプリのビルドを Windows ランナーで回す。

## [1.0.0] — 2026-08-12

- 初回公開。
