# 公開手順（はじめての方向け）

上から順にやれば公開できます。**所要 30〜60分**くらいです。

準備はすべて済んでいます。あなたがやるのは以下だけです。

---

## STEP 1　名前を書き入れる（5分）

AGPLv3 では「誰が著作権を持っているか」を書く必要があります。
いまは `★あなたの名前またはハンドル名★` という仮の文字が入っているので、
そこを実際の名前に置き換えます。

本名でなくて構いません。ハンドル名や活動名で大丈夫です。

### やること

`dist\THIRD-PARTY-NOTICES.txt` をメモ帳で開いて、上から数行目の

```
Copyright (C) 2026  ★あなたの名前またはハンドル名★
```

を、たとえばこう書き換えます。

```
Copyright (C) 2026  hakusekirei
```

同じファイルの下の方にある

```
  ソースコード： ★公開先の URL★
```

は STEP 3 が終わってから埋めます。いまは飛ばしてください。

---

## STEP 2　GitHub のアカウントを作る（10分）

ソースコードを置く場所です。無料です。

1. https://github.com/ を開く
2. 「Sign up」をクリック
3. メールアドレス・パスワード・ユーザー名を入力
4. 届いたメールで認証

すでにお持ちなら飛ばしてください。

---

## STEP 3　ソースコードを GitHub に置く（15分）

**すでにコミットまで済ませてあります。** あとはアップするだけです。

### 3-1　GitHub 側で置き場所を作る

1. GitHub にログインして、右上の「+」→「New repository」
2. 「Repository name」に `kantan-voice-changer` と入力
3. 「Public」を選ぶ（**Private だと AGPL の条件を満たせません**）
4. 下の「Add a README file」などは**すべてチェックを外す**
   （こちらに既にファイルがあるため）
5. 「Create repository」

### 3-2　アップロードする

作成後の画面に出てくるコマンドのうち、`git remote add origin ...` の行を
コピーしてください。こんな形です。

```
https://github.com/あなたのユーザー名/kantan-voice-changer.git
```

そうしたら、**PowerShell を開いて**以下を順に実行します。
`★` の部分だけ、いまコピーした URL に差し替えてください。

```powershell
cd D:\vcdev
git remote add origin ★https://github.com/あなたのユーザー名/kantan-voice-changer.git★
git push -u origin main
```

初回はブラウザが開いて GitHub のログインを求められます。許可してください。

うまくいけば、GitHub のページを再読み込みするとソースが並んでいます。

> **メモ**：`D:\vcdev` は `D:\SynologyDrive\簡単ボイチェン` と同じ場所です。
> フォルダ名に日本語が入っているとビルドが壊れるため、別名を付けてあります。

### 3-3　URL を書き戻す

`dist\THIRD-PARTY-NOTICES.txt` の

```
  ソースコード： ★公開先の URL★
```

を、いま作った GitHub の URL に書き換えます。

書き換えたら、ZIP を作り直します（STEP 4 へ）。

---

## STEP 4　配布用 ZIP を作り直す（2分）

STEP 1 と 3-3 でテキストを直したので、ZIP に反映します。
PowerShell で以下を実行するだけです。

```powershell
cd D:\vcdev
Copy-Item dist\THIRD-PARTY-NOTICES.txt dist\公開版\ -Force
Remove-Item dist\簡単ボイチェン_v1.0.zip -ErrorAction SilentlyContinue
Compress-Archive -Path dist\公開版\* -DestinationPath dist\簡単ボイチェン_v1.0.zip
```

`dist\簡単ボイチェン_v1.0.zip` ができます。これが配布物です。

---

## STEP 5　BOOTH に出す（15分）

1. https://booth.pm/ にログイン（無ければアカウント作成）
2. 「商品を登録する」
3. 商品名・説明文は `BOOTH掲載文.md` の内容をコピーして貼り付け
   - `★` の箇所（ライセンス・ソース URL）を埋めてください
4. 価格は **0円**（無料配布）を選択
5. 商品ファイルに `簡単ボイチェン_v1.0.zip` をアップロード
6. サムネイル画像を用意（アプリのスクリーンショットで十分です）
7. 公開

---

## STEP 6（任意）　コード署名

無署名だと、利用者の環境で
**「WindowsによってPCが保護されました」** という警告が必ず出ます。

- 回避手順（「詳細情報」→「実行」）は説明文に書いてあるので、
  やらなくても配布は可能です
- ただし、ここで諦めてしまう人は一定数います
- 個人でも証明書は取得できます（年1〜2万円程度から）

最初は署名なしで出して、反応を見てから考えても構いません。

---

## よくあるつまずき

| 症状 | 対処 |
|---|---|
| `git push` で「認証に失敗」 | ブラウザが開くのでログインを許可。出ない場合は GitHub Desktop を使うと簡単です |
| `git` が見つからない | https://git-scm.com/ からインストール |
| Private にしてしまった | GitHub のリポジトリ設定 → Danger Zone → Change visibility → Public |
| ZIP に余計なフォルダが入る | `dist\公開版\*` の `*` を忘れていないか確認 |

---

## 更新版を出すとき

ソースを直したあと、PowerShell で：

```powershell
cd D:\vcdev
git add -A
git commit -m "変更内容を一言で"
git push
```

ビルドと ZIP 作成は `README.md` のビルド手順を参照してください。

---

## 参考：なぜ AGPLv3 なのか

無料配布ならこれが**いちばん手続きが少ない**からです。

| | AGPLv3（今回） | ソース非公開の場合 |
|---|---|---|
| JUCE | 無料・手続きなし | ライセンス取得が必要 |
| ASIO | GPLv3 の選択肢を使える | Steinberg の署名入り同意書が必要 |
| 費用 | 0円 | 場合により発生 |
| 条件 | ソース公開 | — |

ソースを公開する代わりに、費用も手続きもゼロで済みます。

なお AGPLv3 の「ネットワーク越しの利用者にソースを提供する義務」は、
**このアプリのようにパソコン上で動くソフトには実質関係ありません**
（サーバーで動かして他人に使わせる場合の条項です）。
